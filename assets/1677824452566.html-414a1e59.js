import{_ as e,Z as n,$ as d,a4 as i}from"./framework-4a2165a0.js";const a={},l=i(`<p>npx 是 npm v5.2.0 引入的一条命令（ npx ），是一个 npm 包执行器。我们可以使用 npx 来执行各种命令。</p><p>主要有以下特点：</p><ul><li>临时安装可执行依赖包，不用全局安装，不用担心长期的污染。</li><li>可以执行依赖包中的命令，安装完成自动运行。</li><li>自动加载 <code>node_modules</code> 中依赖包，不用指定 <code>$PATH</code> 。</li><li>可以指定 node 版本、命令的版本，解决了不同项目使用不同</li></ul><p>npx 执行流程如下：</p><ol><li>到 <code>node_modules/.bin</code> 路径检查对应的命令是否存在，找到之后执行；</li><li>没有找到，就去环境变量 <code>$PATH</code> 里面，检查对应命令是否存在，找到之后执行;</li><li>还是没有找到，自动下载一个临时的依赖包最新版本在一个临时目录，然后再运行命令，运行完之后删除，不污染全局环境。</li></ol><p>安装过程很简单，直接运行以下命令即可</p><div class="language-undefined line-numbers-mode" data-ext="undefined"><pre class="language-undefined"><code>npm install npx -g
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><h3 id="npx-使用场景" tabindex="-1"><a class="header-anchor" href="#npx-使用场景" aria-hidden="true">#</a> npx 使用场景</h3><ul><li>执行本地命令</li></ul><p>比如项目下安装了 mocha ，可以直接用 npx 执行</p><div class="language-undefined line-numbers-mode" data-ext="undefined"><pre class="language-undefined"><code>npx mocha --version
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><ul><li>一次性执行命令</li></ul><p>比如以下命令，npx 将 create-react-app 下载到一个临时目录，使用以后再删除</p><p>不用全局安装 create-react-app ，运行后不会污染全局环境</p><div class="language-undefined line-numbers-mode" data-ext="undefined"><pre class="language-undefined"><code>npx create-react-app myApp
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><ul><li>切换 node 版本</li></ul><p>要运行的命令不兼容当前的 node 版本时，可使用 npx 切换版</p><p>npx 的 -p 选项指定要安装的包，并将其添加到正在运行的 $PATH 中</p><div class="language-css line-numbers-mode" data-ext="css"><pre class="language-css"><code>npx node@12 -v
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><ul><li>开启静态服务器</li></ul><p>使用 http-server 或者 serve 开启静态服务器。方便本地预览打包后的项目</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code>npx http-server        <span class="token comment"># 默认返回根目录下index.htmlnpx http-server -p 3000   # 也可以直接指定端口</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div>`,22),s=[l];function c(p,r){return n(),d("div",null,s)}const o=e(a,[["render",c],["__file","1677824452566.html.vue"]]);export{o as default};
